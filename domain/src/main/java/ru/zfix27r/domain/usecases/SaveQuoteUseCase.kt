package ru.zfix27r.domain.usecases

/*
class SaveQuoteUseCase @Inject constructor(private val repository: ContentRepository) {
    suspend fun execute(quoteModel: QuoteModel) {
*/
/*        if (quote.id > 0) contentRepository.update(quote)
        else contentRepository.insert(quote)*//*

    }
}*/
